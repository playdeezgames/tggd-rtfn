Public Module StatisticType
    Public ReadOnly MazeColumn As String = NameOf(MazeColumn)
    Public ReadOnly MazeColumns As String = NameOf(MazeColumns)
    Public ReadOnly MazeRow As String = NameOf(MazeRow)
    Public ReadOnly MazeRows As String = NameOf(MazeRows)
    Public ReadOnly DestinationLocationId As String = NameOf(DestinationLocationId)
    Public ReadOnly Satiety As String = NameOf(Satiety)
    Public ReadOnly Health As String = NameOf(Health)
    Public ReadOnly Points As String = NameOf(Points)
    Public ReadOnly Score As String = NameOf(Score)
End Module
